name: 'Get Submodule'
description: 'Downloads and builds a git submodule'
inputs:
  definitions:
    description: 'CMake definitions'
    required: true
    default: ''
  input_var:
    description: 'CMake definitions'
    required: true
    default: ''
outputs:
  output_var:
    description: "CMake definitions output"
    value: ${{ steps.clone.outputs.output_var }}
runs:
  using: "composite"
  steps: 
    - name: Clone
      shell: bash
      id: clone
      run: |
        echo "::set-output name=output_var::${{ inputs.definitions }}${{ inputs.input_var }}"
        #$(echo $tmpDef)"

